{#
/**
 * @file
 * NovaCast: Main menu block wrapper.
 *
 * Overrides the system menu block for the main navigation;
 * removes the default block wrapper markup so the <nav> in
 * page.html.twig acts as the landmark.
 */
#}
{{ content }}
